// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/front-bet-scrapper',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
